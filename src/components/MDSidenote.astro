---
interface Props {
  name: string;
}

const { name } = Astro.props;
---

<div class="sidenote-container">
  <slot name="sidenote" /><slot />
</div>

<style>
  /* .sidenote-container {
    position: relative;
  }

  .sidenote-content {
    position: absolute;
    float: right;
    clear: right;
  }

  .main-content {
    position: relative;
    width: 100%;
  } */
</style>
