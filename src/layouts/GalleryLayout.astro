---
import Layout from "@layouts/Layout.astro";

interface Props {
  title: string;
}

const { title } = Astro.props;
---

<Layout title={title}>
  <slot name="header" slot="header" />

  <!-- Main gallery content - no gridContainer, no ProseWrapper -->
  <slot />
</Layout>

<style>
  /* Gallery-specific spacing/layout rules */
  /* Galleries should be full-width, so we keep this minimal */
</style>
